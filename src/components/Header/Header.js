import React from 'react';

import styles from '../../Styles/Styles';

const header = () => (
    <div style={styles.header}>
        <p style={styles.headerP}>Contacts</p>
    </div>
);

export default header;